import styled from 'styled-components';
import { COLORS } from '../constants';

const Accesories = () => {
  return <Container>TECHNIKA</Container>;
};

const Container = styled.div`
  background: ${COLORS.third};
  height: 300px;
`;

export default Accesories;
